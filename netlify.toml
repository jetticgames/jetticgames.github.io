# Netlify configuration for Jettic Games v2.0
[build]
  publish = "frontend"
  
# Headers for security and performance
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
    Permissions-Policy = "geolocation=(), microphone=(), camera=()"
    # Add cache control for API responses
    Cache-Control = "public, max-age=300"

[[headers]]
  for = "*.js"
  [headers.values]
    Cache-Control = "public, max-age=31536000"

[[headers]]
  for = "*.css"
  [headers.values]
    Cache-Control = "public, max-age=31536000"

[[headers]]
  for = "*.json"
  [headers.values]
    # Reduced cache time since we now primarily use backend APIs
    Cache-Control = "public, max-age=60"

[[headers]]
  for = "/images/*"
  [headers.values]
    Cache-Control = "public, max-age=86400"

# Redirects for SPA routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Proxy backend API requests if using same domain (optional)
# [[redirects]]
#   from = "/api/*"
#   to = "https://your-backend-worker.workers.dev/api/:splat"
#   status = 200
#   force = true

# [[redirects]]
#   from = "/proxy"
#   to = "https://your-backend-worker.workers.dev/proxy"
#   status = 200
#   force = true
